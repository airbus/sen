# Everything the documentation build needs, pinned exactly so a build is
# reproducible and an upgrade is a reviewed change. Every entry here is enabled
# in mkdocs.yml; anything that stops being used comes out.
mkdocs==1.6.1
# 9.7.7 is a security release: it fixes a DOM-based XSS in search suggestions.
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
mkdocs-git-revision-date-localized-plugin==1.5.1
mkdocs-glightbox==0.5.2
mkdocs-redoc-tag==0.2.0
mkdocs-callouts==1.16.0
# The version workflow deploys with mike. It was installed separately by CI,
# which meant a local build warned about a provider it did not have.
mike==2.1.3
