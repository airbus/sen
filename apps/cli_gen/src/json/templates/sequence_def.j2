    "{{ senQual }}": {
      "$schema": "https://json-schema.org/draft-07/schema",
      "description": "{{ description }}",
      "type": "array",
      "items": { {{ jsonElemSchemaType }} }{% if fixedSize or isBounded %},{% endif %}
## if fixedSize
      "minItems": {{ maxSize }},
      "maxItems": {{ maxSize }}
## else if isBounded
      "maxItems": {{ maxSize }}
## endif
    }
