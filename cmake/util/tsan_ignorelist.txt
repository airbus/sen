# TODO(SEN-1503):  fix deadlock
deadlock:sen::impl::RemoteObject::makeRemoteCall*

# TODO(SEN-1504):  fix deadlock
deadlock:sen::kernel::impl::RemoteParticipant::onObjectsAdded*

# TODO(SEN-1505):  fix deadlock
deadlock:sen::kernel::impl::Bus::isObjectNameUsedLocally*

# TODO(SEN-1506):  fix deadlock
deadlock:sen::components::ether::EtherTransport::detectedNewProcess*

# TODO(SEN-1510): fix thread leak
thread:sen::components::ether::EtherTransport::start*

# TODO(SEN-1507):  fix race
race:sen::test::TesterRemoteProxy::getReady*

# TODO(SEN-1508):  fix race
race:sen::kernel::impl::LocalParticipant::localSubscriberAdded

# TODO(SEN-1509):  fix race
race:sen::ObjectProvider::notifyObjectsAdded

# TODO(SEN-1511):races with ::clear (e2e_test)
race:sen::impl::WorkQueueImpl::push

# TODO(SEN-1501): spdlog + pybind11 interaction
race:pybind11::scoped_interpreter::scoped_interpreter

# TODO(SEN-1608): signal handling thread safety / reentrance
signal:sen::impl::defaultCheckHandler
signal:sen::impl::terminateHandler

# TODO(SEN-1608): race during rnd number generation
race:std::mersenne_twister_engine

# TODO(SEN-1612): race in asio is_open
race:asio::detail::reactive_socket_service_base::is_open

# TODO(SEN-1613): heap-use-after-free
race:moodycamel::ConcurrentQueue<std::unique_ptr<std::vector<unsigned char, std::allocator<unsigned char>>, std::default_delete<std::vector<unsigned char, std::allocator<unsigned char>>>>, moodycamel::ConcurrentQueueDefaultTraits>::try_get_block_from_initial_pool

# TODO(SEN-1622): data race in ProcessHandler::shutdownSockets
race:sen::components::ether::ProcessHandler::shutdownSockets

# TODO(SEN-1647): deadlock found running object_sync tests with TSAN
deadlock:sen::kernel::impl::Session::lock

# TODO(SEN-1648): data race found running object_sync tests with TSAN
race:sen::kernel::impl::RemoteParticipant::startTrackingProxy

# TODO(SEN-1649): data race found running object_sync tests with TSAN
race:sen::impl::PropertyFlags::currentIndex

# TODO(SEN-1678): data race found while running object_sync_writable_props_two_processes with TSAN
race:sen::impl::RemoteObject::sendCall

# TODO(SEN-1679): data race found while running object_sync_writable_props_two_processes with TSAN
# Issue with writable properties: setNext method can be called from another thread and the ProxyObject mutex is not locked (property buffers unprotected)
race:object_sync::TestObjectRemoteProxy::setNextWritableProp

# TODO(SEN-SEN-1706): data race found while running object_sync_confirmed_events_single_process with TSAN
race:sen::impl::EventBuffer::addConnection

# TODO(SEN-1511): the ::clear side of the same race. Runner::stopThread clears the
# work queue during Executor::shutDown while a worker thread is still reading it.
# Found by RestE2EFixture.invoke_method, but the race is in the kernel shutdown path.
race:sen::impl::WorkQueueImpl::clear

# The pre-redesign send path, which the concurrency redesign removes rather than
# repairs. RemoteInterestsManager owns the ObjectUpdate buffers, and cli_run tears
# them down while a producer thread is still resetting them; the manager's own
# remove-vs-send is the same story. Suppressed to keep the nightly readable, not
# because the race is benign: destruction racing use can crash a process at exit.
# Drop these when `git grep -c RemoteInterestsManager origin/main -- libs/kernel/src`
# returns nothing, which is the merge that deletes the code.
#
# ResizableHeapBlock::~ResizableHeapBlock summarises three of these and is left
# out on purpose: it is core, used across twelve files, and suppressing its
# destructor would hide races that have nothing to do with this. Each of those
# three has ObjectUpdate::reset on the other side, so the first entry covers them.
race:sen::kernel::impl::ObjectUpdate::reset*
race:sen::kernel::impl::ObjectUpdate::~ObjectUpdate*
race:sen::kernel::impl::RemoteInterestsManager::removeObjectUpdatesByInterest*
race:sen::kernel::impl::RemoteInterestsManager::sendObjectUpdates*
